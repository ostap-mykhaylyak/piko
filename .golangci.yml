run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell
    - gocritic
    - revive

issues:
  exclude-rules:
    # Test helpers legitimately ignore some errors.
    - path: _test\.go
      linters:
        - errcheck
